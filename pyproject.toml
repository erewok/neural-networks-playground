[project]
name = "neural-networks-study"
version = "0.1.0"
description = "Learning neural networks by fixing broken ones"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.11.1",
    "rich>=13.7",
    "seaborn>=0.13.2",
]
